# Copy to `.env` in this same folder (repo root).
# Main-process only. Never put this key in renderer / browser code.

OPENAI_API_KEY=

# Later: Python detector. Unused while SENTINEL_DETECTOR=mock.
DETECTOR_URL=http://127.0.0.1:8000/analyze
SENTINEL_DETECTOR=mock
