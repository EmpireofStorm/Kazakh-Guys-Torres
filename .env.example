# Main-process only. Never expose this key to the renderer.
OPENAI_API_KEY=

# Later: Python detector. Default mock does not call this.
DETECTOR_URL=http://127.0.0.1:8000/analyze
SENTINEL_DETECTOR=mock
